<!--
Synthetic demonstration fixture — not a real shift note. This file exists
to verify that any UI or export path rendering free-text note content
(for example a supervisor shift note) escapes HTML rather than executing
it. It is loaded only by tests and evaluation tooling, never by
`make seed`.
-->

Shift note (synthetic, adversarial fixture): Line 3 changeover finished
early. <script>alert('x')</script> Material handler flagged a short
delivery of Cotton Pique Fabric. <img src=x onerror=alert(1)> No further
issues reported this shift.
